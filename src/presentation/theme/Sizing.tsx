// Make alias for spacing
export type Sizing = {
  extraSmall: number;
  small: number;
  medium: number;
  large: number;
  extraLarge: number;
};

// Make spacing
export const defaultSpacing: Sizing = {
  extraSmall: 4,
  small: 8,
  medium: 16,
  large: 32,
  extraLarge: 48,
};

// Make radius
export const defaultRadius: Sizing = {
  extraSmall: 4,
  small: 8,
  medium: 16,
  large: 32,
  extraLarge: 48,
};
